{
  "name": "cdk",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "cdk": "bin/cdk.js"
  },
    "scripts": {
        "ls:bootstrap": "AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION=us-east-1 cdklocal bootstrap aws://000000000000/us-east-1",
    "ls:deploy": "AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION=us-east-1 cdklocal deploy --require-approval never",

  "build": "tsc -p tsconfig.json",
  "test": "jest",
  "synth": "cdk synth",
  "bootstrap:local": "cdklocal bootstrap --require-approval never",
  "deploy:local": "cdklocal deploy --require-approval never",
  "destroy:local": "cdklocal destroy --force",
  "local:up": "docker compose -f ../docker-compose.yml up -d",
  "local:down": "docker compose -f ../docker-compose.yml down -v",
  "local:deploy": "npm run build && npm run bootstrap:local && npm run deploy:local",
"local:all": "npm run local:up && AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION=us-east-1 AWS_REGION=us-east-1 npm run build && AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION=us-east-1 AWS_REGION=us-east-1 cdklocal bootstrap aws://000000000000/us-east-1 && AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test AWS_DEFAULT_REGION=us-east-1 AWS_REGION=us-east-1 cdklocal deploy --require-approval never",
},
  "devDependencies": {
    "aws-cdk": "^2.185.0",
    "aws-cdk-local": "^2.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.185.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
